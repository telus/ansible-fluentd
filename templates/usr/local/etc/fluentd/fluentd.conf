include {{ fluentd_config_include_root }}/*.conf

<source>
  type forward
</source>

<match debug.**>
  type stdout
</match>

{% if 'elasticsearch' in fluentd_plugins %}
<match reformed.elasticsearch.**>
  type elasticsearch
  host {{ fluentd_elasticsearch_host }}
  include_tag_key true
</match>
{% endif %}

{% if 'influxdb' in fluentd_plugins %}
<match reformed.influxdb.**>
  type influxdb
  host {{ fluentd_influxdb_host }}
</match>
{% endif %}

<match reformed.**>
  type null
</match>

<match **>
  type record_reformer
  tag reformed.${tag}
  <record>
    host_role {{ system_role }}
    host_project {{ project }}
    host_environment {{ environment_tier }}
  </record>
</match>
